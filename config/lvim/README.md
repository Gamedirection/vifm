This directory tracks the user-facing LunarVim configuration intended for deployment.

Contents:
- `config.lua`: personal LunarVim configuration
- `lazy-lock.json`: pinned plugin lockfile

The LunarVim application source itself is referenced separately as the optional
`submodules/lvim` submodule.
